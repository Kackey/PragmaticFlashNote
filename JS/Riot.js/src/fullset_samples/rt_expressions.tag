<rt_expressions>
    <h1>Expressions</h1>
    <ul>
        <li>{ name }</li>
        <li>{ subtitle || "サブタイトルないよー" }</li>
        <li>{ flag ? 'ready' : 'loading' }</li>
        <li>{ new Date() }</li>
        <li>{ Math.round(64.5) }</li>
    </ul>
    this.name = 'phi';
    this.flag = false;
</rt_expressions>